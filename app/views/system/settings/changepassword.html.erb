<% content_for :main do %>

  <div class="form">
  	
		<div class="header">
			<h2>Change Password</h2>
			<p>Please create a new secure password for your account</p>
		</div>
    
    <% form_for @user, :url => changepassword_settings_url, :method => 'PUT' do |f| %>
    
      <div class="body">
  
        <dl>
          
          <%= f.password_field_control @user, :password, { :text => 'Password', :class => 'required' }, { :autocomplete => 'off' } %>

          <%= f.password_field_control @user, :password_confirmation, { :text => 'Confirm', :class => 'required' }, { :autocomplete => 'off' } %>
				
				</dl>

      </div>
    
      <div class="buttons">
    
        <%= f.submit "Change", { :class => "button" } %>
        
      </div>
    
    <% end %>
  
  </div>

<% end %>